<!doctype html>
<html>

<head>
  <meta charset="utf-8">
  <title>Auth</title>
</head>

<body>Completing login…</body>
<script>
  (function () {
    const msg = {
      type: "SESSION_TOKEN",
      token: {{ .token | toJSON }},
      user: {{ .user | toJSON }},
    };
    const target = {{ .origin | toJSON }}
    try {
      if (window.opener && typeof window.opener.postMessage === "function") {
        window.opener.postMessage(msg, target)
      }
    } finally {
      try {
        window.close()
      } catch (e) { }
    }
  })()
</script>

</html>
